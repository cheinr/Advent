plugins {
    id "com.moowork.node" version "0.13"
}

apply plugin: 'base'

node {
    version = '6.9.0'
    npmVersion = '3.10.9'
    download = true
}

task npmTest(type: NpmTask) {
    group = 'node'
    args = ['test']
}

task npmStart(type: NpmTask) {
    group = 'node'
    args = ['start']
}